//! New Body — Rust core engine.
//!
//! High-performance, dependency-light re-implementation of the surrogate
//! control plane described in `source.html`. Mirrors the Python package's
//! module layout so the two stay in sync.

pub mod cat8;
pub mod chassis;
pub mod patch_panel;
pub mod poe;
pub mod raw;
pub mod render;
pub mod surrogate;
